<html>
    <head>
        <title>{safe data::title}</title>
    </head>
    <body>
        <p>{safe data::text}</p>
        {each data::projects {fn (project)
            <a href="{safe project.url}">{safe project.name}</a>
            <p>{safe project.description}</p>
        }
        "No projects"}
    </body>
</html>